import React, { Component } from 'react';
import { KEY } from '../../config';

class Home extends Component {
  render() {
    console.log(KEY)
    return (
      <div>
        
      </div>
    );
  }
}

export default Home;